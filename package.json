{
  "name": "speedtest-app",
  "version": "2.0.0",
  "description": "Speed Test Application with React frontend",
  "scripts": {
    "build": "cd project && npm ci && npm run build",
    "dev": "cd project && npm run dev",
    "start": "cd project && npm run preview"
  },
  "dependencies": {
    "framer-motion": "^12.16.0",
    "html2canvas": "^1.4.1",
    "jspdf": "^3.0.1",
    "jspdf-autotable": "^5.0.2",
    "lucide-react": "^0.513.0",
    "qrcode": "^1.5.4",
    "react-hot-toast": "^2.5.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

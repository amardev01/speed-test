# Environment Configuration for Speed Test Application

# API Base URL - Set this to your production domain
# For development: http://localhost:8080
# For production: https://your-domain.com
VITE_API_BASE_URL=http://localhost:8080

# WebSocket URL - Set this to your production WebSocket endpoint
# For development: ws://localhost:8080
# For production: wss://your-domain.com
VITE_WS_URL=ws://localhost:8080

# Production Settings
# Set to 'production' when deploying
NODE_ENV=development

# Server Configuration
# Port for the PHP server (used in development)
PORT=8080

# CORS Settings
# Set allowed origins for production
CORS_ORIGIN=*

# LibreSpeed Backend Settings
# These are handled by the PHP backend files
# No additional configuration needed for basic setup